= simple_form_for @auction_part, html: { multipart: true} do |f|
  - if @auction_part.errors.any?
      #errors
        %p
          = @auction_part.errors.count
          Prevent this auction item from saving
        %ul
          - @auction_part.errors.full_messages.each do |msg|
            %li= msg
    .panel-body
      = f.input :title, input_html: { class: 'form-control' }
      = f.input :description, input_html: { class: 'form-control' }
    
    = f.button :submit, class: "btn btn-primary"